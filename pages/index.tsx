import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
	font-size: 21px;
	color: gray;
`;

const index: React.FC = () => {
	return <Container>플레이캠핑 사이트를 만들어보자 :)</Container>;
};

export default index;
